{% extends 'base.html.twig' %}

{% block body %}
    <img src="{{ asset('image/Tunisia.png') }}" alt="Mon logo">
    <h1>Hello from Tunisia</h1>
    <p>It's currently {{ 'now'|date('h:i A') }}</p>
{% endblock %}
